#include <cstdio>
using namespace std;

int main() {
    int n,m,i,sum=0,t=0;
    scanf("%d%d",&n,&m);
    for(i=1;i<=m;i++)
    {
        scanf("%d%d%d",&tmp.u,&tmp.v,&tmp.len);
        q.push(tmp);
    }

    return 0;
}