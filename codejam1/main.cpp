#include<cstdio>
#include<vector>
#include<cstring>
#include<cstdlib>
#include<cmath>
#include<iostream>
#include<algorithm>
#define mm(a,i) memset(a,i,sizeof(a))
#define MAXN 1000000007
using namespace std;
int main()
{
    int T,i,j,round=1;
    scanf("%d",&T);
    while(round<=T)
    {
        string a,b;
        gets(a);
        gets(b);
        for(i=1;i<=4;i++)
        {

        }
        round++;
    }
}